<Window x:Class="BitDoFixer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="8BitDo Fixer" Height="600" Width="900"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Status Cards -->
            <RowDefinition Height="*"/>    <!-- Logs -->
            <RowDefinition Height="Auto"/> <!-- Actions -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" materialDesign:ElevationAssist.Elevation="Dp3">
            <DockPanel>
                <materialDesign:PackIcon Kind="Gamepad" Width="30" Height="30" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Text="8BitDo Fixer"/>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Status Dashboard -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Card 1: Connection Status -->
            <materialDesign:Card Margin="0,0,10,0" Padding="16" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="REMAPPER STATUS" FontSize="10" FontWeight="Bold" Opacity="0.7" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <materialDesign:PackIcon Kind="Bluetooth" Width="40" Height="40" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="TxtRemapperStatus" Text="Scanning..." FontSize="24"/>
                            <TextBlock x:Name="TxtDeviceInfo" Text="Searching for D-Input Device..." FontSize="12" Opacity="0.7"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Card 2: Battery -->
            <materialDesign:Card Grid.Column="1" Margin="10,0,0,0" Padding="16" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="BATTERY LEVEL" FontSize="10" FontWeight="Bold" Opacity="0.7" Foreground="{DynamicResource SecondaryHueMidBrush}"/>
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Battery Icon/Gauge -->
                        <Grid Width="60" Height="60">
                             <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}"
                                          Value="{Binding BatteryPercentage, FallbackValue=75}"
                                          Maximum="100" Width="50" Height="50"/>
                             <materialDesign:PackIcon Kind="BatteryCharging"
                                                      Width="24" Height="24"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                            <TextBlock x:Name="TxtBatteryLevel" Text="--%" FontSize="32" FontWeight="Bold"/>
                            <TextBlock x:Name="TxtBatteryDevice" Text="No 8BitDo Connected" FontSize="12" Opacity="0.7"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Logs Console -->
        <Border Grid.Row="2" Margin="20,0,20,10" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="4" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1">
            <Grid>
                <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="TxtLogs" Margin="10" IsReadOnly="True" 
                             TextWrapping="Wrap" BorderThickness="0" FontFamily="Consolas" FontSize="12"
                             Foreground="{DynamicResource MaterialDesignBody}"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Actions Footer -->
        <Grid Grid.Row="3" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="v0.0.1 • github.com/bezelye404" VerticalAlignment="Center" Opacity="0.5" FontStyle="Italic"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="BtnStop" Click="BtnStop_Click" Style="{DynamicResource MaterialDesignOutlinedButton}" Content="STOP SERVICE" Margin="0,0,10,0" IsEnabled="False"/>
                <Button x:Name="BtnStart" Click="BtnStart_Click" Style="{DynamicResource MaterialDesignRaisedButton}" Content="START SERVICE"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
